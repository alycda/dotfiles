name: Entity diff

on: pull_request

permissions:
  contents: read
  pull-requests: write

jobs:
  entity-diff:
    name: Sem entity diff
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: Ataraxy-Labs/sem/action@v0.15.1
