# felixia: linked to ~/.homebrew/brew.env by `mise run brew-pin`; see config.toml.
# bin/brew loads only HOMEBREW_* lines from this file, so comments are safe.
HOMEBREW_NO_AUTO_UPDATE=1
HOMEBREW_NO_AUTOREMOVE=1
