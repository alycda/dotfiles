# ~/hermes-boxed/workspace.env — workspace UI secrets, never tracked.
HERMES_API_TOKEN=        # == state/.env API_SERVER_KEY
HERMES_PASSWORD=         # workspace login password (rotate from May-era value)
COOKIE_SECURE=false
