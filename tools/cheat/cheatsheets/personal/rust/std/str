# str Methods

## Conversion & Validation
from_utf8(&[u8]) -> Result<&str, Utf8Error>
from_utf8_mut(&mut [u8]) -> Result<&mut str, Utf8Error>
as_bytes() -> &[u8]
chars() -> Chars
bytes() -> Bytes
char_indices() -> CharIndices
is_ascii() -> bool
is_empty() -> bool
len() -> usize

## Splitting
split_at(usize) -> (&str, &str)
lines() -> Lines
split(Pattern) -> Split
rsplit(Pattern) -> RSplit
splitn(usize, Pattern) -> SplitN
rsplitn(usize, Pattern) -> RSplitN
split_terminator(Pattern) -> SplitTerminator
split_whitespace() -> SplitWhitespace
split_ascii_whitespace() -> SplitAsciiWhitespace

## Trimming
trim() -> &str
trim_start() -> &str
trim_end() -> &str
trim_matches(Pattern) -> &str
trim_start_matches(Pattern) -> &str
trim_end_matches(Pattern) -> &str
strip_prefix(Pattern) -> Option<&str>
strip_suffix(Pattern) -> Option<&str>

## Searching
contains(Pattern) -> bool
starts_with(Pattern) -> bool
ends_with(Pattern) -> bool
find(Pattern) -> Option<usize>
rfind(Pattern) -> Option<usize>
matches(Pattern) -> Matches
rmatches(Pattern) -> RMatches
match_indices(Pattern) -> MatchIndices
rmatch_indices(Pattern) -> RMatchIndices

## Transformation
to_uppercase() -> String
to_lowercase() -> String
to_ascii_uppercase() -> String
to_ascii_lowercase() -> String
replace(Pattern, &str) -> String
replacen(Pattern, &str, usize) -> String
repeat(usize) -> String

## Parsing
parse<T>() -> Result<T, T::Err>  where T: FromStr

## Mutable (&mut str)
split_at_mut(usize) -> (&mut str, &mut str)
make_ascii_uppercase()
make_ascii_lowercase()

## Encoding
encode_utf16() -> EncodeUtf16

from https://upsuper.github.io/rust-cheatsheet/
