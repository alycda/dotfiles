ssh admin@$(tart ip gg-validate)   # cirruslabs base images: admin / admin

# mount a host directory into the guest, where it appears under
# /Volumes/My Shared Files; add :ro to keep the guest from writing to it
tart run --dir="$PWD:ro" gg-validate

# Runnable: just -g tart-ssh gg-validate
