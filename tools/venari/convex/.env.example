# Copy to .env on the box (mode 600). Generated there:
#   CONVEX_POSTGRES_PASSWORD=$(openssl rand -hex 24)
# The same value goes into the role in Ghost's cluster (README, "Convex").
CONVEX_POSTGRES_PASSWORD=
