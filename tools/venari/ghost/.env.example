# Copy to .env on the box (mode 600). Both values are generated there:
#   POSTGRES_PASSWORD=$(openssl rand -hex 24)
#   GHOST_API_KEY=gt_$(openssl rand -hex 20)
# The laptop's copy of GHOST_API_KEY is the agenix secret personal/ghost-api-key.age.
POSTGRES_PASSWORD=
GHOST_API_KEY=
